---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# network-monitoring Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-07-07

## Active Technologies
- C# / .NET 10 + .NET Worker runtime, shared domain SeedWork abstractions, tshark capture adapter, console serialization pipeline (Kafka-ready contracts) (003-device-discovery)
- N/A in this increment (discovery emission only) (003-device-discovery)
- Elasticsearch 8.11.4 + Kafka Connect 7.6.1 Elasticsearch Sink, fed from `sessions.detected` (002-session-indexing)
- C# / .NET 10 + .NET Worker/Console runtime, shared SeedWork domain abstractions, tshark CLI integration (001-session-detection)
- C# / .NET 10 + .NET Worker host, shared SeedWork domain, tshark CLI, Confluent.Kafka, Schema Registry Avro serdes (003-device-discovery)
- Apache Kafka reference stack (3 brokers, KRaft) + Schema Registry for emitted `DeviceDetected` events (003-device-discovery)
- C# / .NET 10 + .NET Worker hosting, shared `NetworkMonitoring.Domain`, Confluent.Kafka, Schema Registry Avro serdes, HTTP forwarding (004-device-ingestion)
- N/A in this increment; no database or durable ingestion store is implemented (004-device-ingestion)
- C# / .NET 10 + ASP.NET Core hosting, shared `NetworkMonitoring.Domain`, EF Core PostgreSQL provider (005-device-inventory)
- PostgreSQL for authoritative device inventory persistence (005-device-inventory)
- TypeScript on current Node.js LTS, browser runtime JavaScript + React, Vite, TypeScript, browser Fetch API, lightweight runtime configuration, container static file serving (006-device-management)
- N/A for UI persistence; backend remains authoritative for device inventory (006-device-management)
- C# / .NET 10 for API and background services; JSON connector configuration + ASP.NET Core API hosting, graph database driver for API graph access, Kafka Connect graph sink connector for projection ingest (007-device-communication-graph)
- Graph database (communication projection), existing device inventory store remains authoritative for internal device lifecycle (007-device-communication-graph)
- C# / .NET 10 for API/hosted services; JSON for connector configuration + ASP.NET Core minimal APIs, Microsoft DI/options hosting stack, graph database adapter boundary, Kafka Connect Neo4j sink contract (007-device-communication-graph)
- Graph database for communication projection; existing inventory store remains authoritative for internal devices (007-device-communication-graph)
- C# / .NET 10 (Probe, Integration Console, Backend), TypeScript/React (Frontend) + OpenTelemetry SDK/runtime instrumentation, OpenTelemetry Collector (OTLP), (008-observability-baseline)
- Prometheus TSDB for metrics; trace store via Jaeger; existing operational datastores remain (008-observability-baseline)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for C# / .NET 10

## Code Style

C# / .NET 10: Follow standard conventions

## Recent Changes
- 008-observability-baseline: Added C# / .NET 10 (Probe, Integration Console, Backend), TypeScript/React (Frontend) + OpenTelemetry SDK/runtime instrumentation, OpenTelemetry Collector (OTLP),
- 007-device-communication-graph: Added C# / .NET 10 for API/hosted services; JSON for connector configuration + ASP.NET Core minimal APIs, Microsoft DI/options hosting stack, graph database adapter boundary, Kafka Connect Neo4j sink contract
- 007-device-communication-graph: Added C# / .NET 10 for API and background services; JSON connector configuration + ASP.NET Core API hosting, graph database driver for API graph access, Kafka Connect graph sink connector for projection ingest


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read
`/home/hugo/network-monitoring/specs/007-device-communication-graph/plan.md`
<!-- SPECKIT END -->
