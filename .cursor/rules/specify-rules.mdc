---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# network-monitoring Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-28

## Active Technologies
- C# / .NET 10 + .NET Worker runtime, shared domain SeedWork abstractions, tshark capture adapter, console serialization pipeline (Kafka-ready contracts) (003-device-discovery)
- N/A in this increment (discovery emission only) (003-device-discovery)
- Elasticsearch 8.11.4 + Kafka Connect 7.6.1 Elasticsearch Sink, fed from `sessions.detected` (002-session-indexing)
- C# / .NET 10 + .NET Worker/Console runtime, shared SeedWork domain abstractions, tshark CLI integration (001-session-detection)
- C# / .NET 10 + .NET Worker host, shared SeedWork domain, tshark CLI, Confluent.Kafka, Schema Registry Avro serdes (003-device-discovery)
- Apache Kafka reference stack (3 brokers, KRaft) + Schema Registry for emitted `DeviceDetected` events (003-device-discovery)
- C# / .NET 10 + .NET Worker hosting, shared `NetworkMonitoring.Domain`, Confluent.Kafka, Schema Registry Avro serdes, HTTP forwarding (004-device-ingestion)
- N/A in this increment; no database or durable ingestion store is implemented (004-device-ingestion)
- C# / .NET 10 + ASP.NET Core hosting, shared `NetworkMonitoring.Domain`, EF Core PostgreSQL provider (005-device-inventory)
- PostgreSQL for authoritative device inventory persistence (005-device-inventory)
- TypeScript on current Node.js LTS, browser runtime JavaScript + React, Vite, TypeScript, browser Fetch API, lightweight runtime configuration, container static file serving (006-device-management)
- N/A for UI persistence; backend remains authoritative for device inventory (006-device-management)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for C# / .NET 10

## Code Style

C# / .NET 10: Follow standard conventions

## Recent Changes
- 006-device-management: Added TypeScript on current Node.js LTS, browser runtime JavaScript + React, Vite, TypeScript, browser Fetch API, lightweight runtime configuration, container static file serving
- 005-device-inventory: Added device inventory backend/API planning with PostgreSQL persistence
- 004-device-ingestion: Added Integration Console ingestion planning for `DeviceDetected` forwarding


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
